export interface BookingResponse {
  placeId: string;
  placeTitle: string;
  placeImage: string;
  firstName: string;
  lastName: string;
  guestNumber: string;
  bookedFrom: string;
  bookedTo: string;
}
